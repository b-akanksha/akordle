import { createSlice } from "@reduxjs/toolkit";

const akordleSlice = createSlice({
    name: 'akordle',
    initialState: {},
    reducer: {},
})

export const {} = akordleSlice.actions;

export default akordleSlice.reducer;